node_modules/
*.log
.zendesk-plugin-data/
.DS_Store

# The packed Desktop Extension bundle is a build artifact, produced by `npm run pack`, and so is
# the versioned copy plus SHA-256 sidecar that scripts/audit-bundle.mjs emits once it passes.
*.mcpb
*.mcpb.sha256

# Coverage output from `npm run test:coverage` (lcov reporter).
coverage/

.worktrees/

# Ablated copies of the executor guard, written and removed by its test (see tests/plugin/).
scripts/.mutant-*.mjs
