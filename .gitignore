node_modules/
*.log
.zendesk-plugin-data/
.DS_Store

# The packed Desktop Extension bundle is a build artifact, produced by `npm run pack`.
*.mcpb

# Coverage output from `npm run test:coverage` (lcov reporter).
coverage/

.worktrees/
