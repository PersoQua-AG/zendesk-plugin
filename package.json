{
  "name": "zendesk-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "bin": {
    "zendesk-authorize": "dist/bin/authorize.js",
    "zendesk-remote": "dist/bin/remote.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "npm run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "authorize": "node dist/bin/authorize.js",
    "pack": "node scripts/assert-prod-tree.mjs && npx --yes @anthropic-ai/mcpb@2.1.2 pack . zendesk.mcpb",
    "start:remote": "node dist/bin/remote.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "express": "^5.2.1",
    "express-rate-limit": "^8.6.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "@types/node": "^20.14.0"
  }
}
